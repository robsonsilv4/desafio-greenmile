jwt:
  expiration: 3600000
  secret: DesafioGreenMile
server:
  servlet:
    context-path: /api
spring:
  application:
    name: desafiogreenmile
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/desafio-greenmile
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: false